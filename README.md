# Task Tracker — Frontend

Angular 18 frontend for the Mini Task Tracker. See the [root README](../README.md) for the full project overview, how to run locally, and deployment steps.

Quick start:

```bash
npm install
npm start
```

Runs at `http://localhost:4200`, talking to the API at the URL configured in `src/environments/environment.ts`.
